{
  "name": "dadoonet.github.io",
  "private": true,
  "scripts": {
    "build": "hugo --minify --buildFuture && npx pagefind --site public",
    "index": "npx pagefind --site public",
    "dev": "hugo server --buildFuture --buildDrafts -D"
  },
  "devDependencies": {
    "pagefind": "^1.5.0"
  }
}
